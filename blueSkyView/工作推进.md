## 7月

1. git lab runner CICD搭建
   - [x] 本地单元测试、集成测试搭建
   - [x] 本地渲染回归测试搭建
     - [x] 实现基于vsg+vsg example(vsgHeadless)+catch2 的渲染回归测试用例；
     - [x] 实现基于vsgXchange的 in-process imageCompare功能；
   - [ ] 重构bsv Pro Engine的init、update等逻辑，保证可直接用于测试用例;
     - [ ] TODO ：依赖外协更新vsg相关库并导入，bsv Pro Engine重新正常渲染；不打算在目前版本的bsv Pro状态下写渲染回归测试，回归测试运行时需要clone submodule,外协给的vsg依赖没有作为git 仓库；
   - [x] CICD服务搭建

     - [x] git lab runner安装
     - [x] CICD配置
   - [ ] 推进代码格式化统一
2. bsv pro依赖vsg等项目版本更新
   - vsg库修改较多，**需要外协提供项目git镜像，拿到commit记录及对应分支，重新rebase到vsg最新；**
     - 应该外协来做vsg相关库的更新，我们内部进行更新做的修改无法同步到外协；
   - 代码同步策略制定：
     - 外协提供所有相关库的镜像；每次更新的镜像，理论上应该是也包含官方的branch、commit信息的；与官方库保持同步；
     - 外协对任何库的修改，最低要求是应该自己重建分支WX，在新分支上进行修改，最好是新建一个封装库，在封装的库里修改；
     - 镜像项目导入内网后，内部新建develop分支做平时的开发工作，每次代码更新进来，develop重新rebase WX;
   - 相关概念澄清（外部澄清 OR 我们自己重新定义）
     - VsgCs
     - CsApp
     - 网络同步模块:cigi
3. 同步模块移植
   - [ ] 代码移植完成
     - [x] 线程库替换；
     - [ ] 时钟同步、状态同步
     - [ ] 帧同步、显示同步（软）
     - [ ] 增加依赖引擎的相关接口
     - [ ] 
   - [ ] 增加单元测试、集成测试用例；
     - [ ] Sync模块需要剥离，在测试用例中独立运行；
   - [ ] 增加模拟测试
     - [ ] 创建4个窗口，模拟4个相邻的视点通道；与host通信同步；
   - [ ] 
4. 其他
   - [ ] 推进任意缩放、分辨率下，引擎正常启动运行；
5. 