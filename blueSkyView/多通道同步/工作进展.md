| 工作内容                  |                                                     | deadline |
| ------------------------- | --------------------------------------------------- | -------- |
| KR1.1 BufferCore 协议移植 | KR1.1.1 移植 Buffer、Packet、Parser、BufferProcess  | 7.3      |
| KR1.2 TCP 同步通道        | KR1.2.1 移植 Syn（TCP 连接、SyncStruct、收发包）    | 7.3      |
|                           | KR1.2.2 移植 Sync Thread + NetDataPool              | 7.3      |
| KR1.3 UDP 反馈回传        | KR1.3.1 移植 Feedback Component + Feedback Network  | 7.10     |
|                           | KR1.3.2 移植 FeedbackTerrainQuery                   | 7.10     |
| KR1.4 入站数据处理        | KR1.4.1 移植 Cigi（收包 → 解析 → 回调）             | 7.17     |
|                           | KR1.4.2 移植 Syn Callback 全部指令包处理            | 7.17     |
|                           | KR1.4.3 移植 Syn Component                          | 7.17     |
| KR1.5 帧同步时机对接      | KR1.5.1移植 Cigi 中的对帧与 Barrier 逻辑            | 7.24     |
|                           | KR1.5.2对帧改接到 Vulkan Present 前执行             | 7.24     |
| KR1.6 网络与启动配置      | KR1.6.1移植 CLI 网络参数（GP/RT IP、端口、gpID 等） | 7.31     |
|                           | KR1.6.2移植 GP / RT 配置结构                        | 7.31     |
| KR1.7 联调验证            | KR1.7.1与仿真主机端到端联调                         | 8.7      |

## BufferCore模块梳理

- 该模块仅Synchronization在用，功能较少没必要作为一个独立库；
- 功能基本是数据解析协议；

## 帧同步机制

#### 业务逻辑

1. 时间同步：以host时间为基准，统一仿真时间；
1. 仿真状态同步：姿态、位置，协议（CIGI）
1. 帧同步（核心）：swap group\swap barrier:多gpu机器一同边界交换，帧号对齐，帧率锁定，超时、掉帧时的降级策略（丢帧、重复、外推），解决画面同一帧边界一起刷新；
1. 显示相位同步：同步信号，多机垂直消隐对齐；解决刷新相位一致，不在不同点撕裂；
1. 视点与投影一致：通道划分、视椎、视口、眼点偏移；几何校正、边缘融合、颜色亮度匹配；解决接缝处几何、亮度连续；
1. 延迟与流水线对齐：硬件层面；
1. 集群管控与容错：

#### **软同步方案**

- 统一仿真帧号 + 网络 Barrier + 固定帧率
- 具体方案是什么？

- 优劣势：成本低，适合研发/中低端；跨机仍可能有数毫秒级错位

#### 目前实现方案

1. 服务器向客户端进行时间同步、指令下发；
2. 客户端向服务器进行地形、碰撞、位置等信息的同步，以及指令message的反馈；
3. 时钟同步有host向IG下发的心跳时间
   - 实际下发了帧号，帧号除以帧率，作为各通道的用于仿真模拟的时钟时间；

4. 状态同步
   - 各个IG通道每帧都向host上传HAT\HOT\BLH\GroundType等数据；[为什么需要所有通道都上传？]
   - 只有1号通道每帧上传Position\CMDMessage数据；
   - Host每帧向IG同步什么状态？没有定期的
   - Host在UI事件触发时，向通道同步事件指令；

5. 帧同步：每帧Host向Ig下发TimeStamp，数据内容是帧号[所以这是帧同步还是时钟同步？]
   - 是否有根据帧号进行渲染、present的逻辑？ 暂未发现；
   - host下发的帧号与ig viewer的帧号并不相同，两套帧号、时间体系？
     - host下发的帧号，与数据一起发来，最终换算为仿真模拟时间；
     - viewer自身的时间不控制仿真模拟时间；
   - ig有每帧向host发送本通道viewer帧号的逻辑，有什么用？
   
6. 

## 测试方案

1. 本地创建多个窗口，每个进程通过socket通信，进行同步的模拟测试
1. 增加同步信息的统计
1. 


## 问题列表

1. 服务端viewhost是否保持不变，是否有源码？
2. CIGI是什么？exe的参数RT\GP分别是什么？ GP应该是客户端通道；RT是服务端？
   - CIGI也是数据协议？与buffer core什么区别；
   - **CIGI**：通用图像生成器接口，Host 与 IG 的标准交互（控制、实体、大气等）
3. 为什么需要客户端向服务端反馈？
   - Time这种轮询的信息应该不需要返馈？
   - 指令类的包，需要反馈给服务端，用于在viewhost中显示；
   - 发回的信息包括：地形高度、碰撞检测、BLH、GroundType、Position、CMD Message;
4. Synchronization和BufferCore模块的主要作用是什么
   - 服务端与各通道同步，Time；
   - Command 指令解析、接收，在callback中交与context、attribute执行；
5. 服务器和客户端是如何相互链接的？
   - 客户端在哪个端口监听TCP socket？ 127.0.0.1::18080，可以配置；
   - 客户端在向哪个IP、端口广播 UDP Socket？ 127.0.0.1::16060，可以配置；
6. 为什么要单独开一个SyncThread守护线程给NetDataPool喂数据？
8. 求助外协问题
   - 几台机器连接，是否完全不需要考虑网络抖动的问题？
   - 软同步原理[统一barrier、统一present]，解决的问题[显示同步]；
     - 无 Framelock时的替代方案;保证了软件层面同时调用present，但真正的上屏仍由GPU的vsync信号决定；
     - 不同机器渲染耗时不一致，导致部分机器错过present时间怎么办？
     - 部分机器未及时回复渲染完成ack，host如何下发present信号；
   - 显示同步的方案是？
     - vsync刷新的相位同步：genlock
     - 多个gpu之间同步切换前后缓冲：FrameLock
   - vsg中的CIGI是自定义的，并没有真正使用官方的CIGI库，为什么没用？
9. 

