#### 帧同步

host 广播 frame N 的仿真状态（姿态、位置、时间戳），各 IG通道基于同一帧号渲染各自视锥。

#### 显示同步

1. GenLock: 硬件层面，多个显示器之间同步vsync 相位；
2. Framelock ：硬件层面，控制多 GPU 同时切换前后缓冲； 
3. 软同步：软件层面同时调用present，但真正的上屏时机仍由GPU的vsync信号决定；无 Framelock时的替代方案;
   - 
4. 

