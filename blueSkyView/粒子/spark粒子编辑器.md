## 粒子编辑器设计

1. 数据格式选型 ： XML VS JSON
   - XML问题：
     - 全量序列化：没设置过的属性、对象，也会保存
     - 结构平铺：使用Ref保存引用关系，不符合直觉
     - 类型信息不直观：对象的参数类型、语义需要猜；没有语义层；
2. UI面板自动绑定 ：用“描述驱动 UI”
   - 基本原理：Json数据描述-->反射数据结构-->UI
3. 

## 

1. Spark 负责模拟，OSG负责渲染
   - 关闭Spark Renderer
   - 每帧获取粒子数据
   - 数据映射到OSG Geometry
     - 每帧从CPU更新VBO
     - instancing
2. 
   - 









